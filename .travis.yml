language: java
sudo: false
cache:
  directories:
  - "$HOME/.m2"
jobs:
  include:
  - stage: test
    script:
    - "./mvnw -B verify"
jdk:
- openjdk11

before_script:
- curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
- chmod +x ./cc-test-reporter
- ./cc-test-reporter before-build

after_script:
- JACOCO_SOURCE_PATH="api-common/src/main/java api-example/src/main/java" ./cc-test-reporter format-coverage api-example/target/site/jacoco/jacoco.xml --input-type jacoco
- ./cc-test-reporter upload-coverage
